<block wx:if="{{$taroCompReady}}">
    <view class="{{curtainClass}}" style="{{anonymousState__temp}}" catchtap="_stopPropagation">
        <view class="at-curtain__container">
            <view class="at-curtain__body">
                <slot></slot>
                <view class="{{btnCloseClass}}" catchtap="onClose" data-e-tap-so="this"></view>
            </view>
        </view>
    </view>
</block>